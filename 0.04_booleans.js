// Bronze
// Create three booleans about yourself.(hasGlasses, isTall, etc)
// Print each one to the console.
var hasGlasses = true;
var isTall=false;
var isSlender=true;

console.log(hasGlasses);
console.log(isTall);
console.log(isSlender);

// Silver
// Create three booleans about yourself.
// Make them two truths and a lie.
// Print them to the console with descriptors. Example below:

console.log("I wear glasses is "+ hasGlasses);
console.log("I am tall is "+ isTall);

// Gold
// Look up boolean operators.Try and combine two booleans using this.
// comparison operators in JS
//= assigns information into a variable
//== evaluates values (returns true or false). equal value.
//=== evaluates values AND data type of variable. equal value adn type.
// != not equal value
// !== not equal value and type
// > greater than
// < less than
// >= greater than or equal to
// <= less than or equal to
// && and
// || or
// ! not



